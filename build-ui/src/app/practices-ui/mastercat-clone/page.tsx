'use client';
import React, { ReactElement } from 'react';
import { PageWrapper } from './components/components.style';
import { HeaderTopBar } from './components';

const MasterCatClone: React.FC = (): ReactElement => {
  return (
    <PageWrapper>
      <HeaderTopBar />
      page
    </PageWrapper>
  );
};

export default MasterCatClone;
